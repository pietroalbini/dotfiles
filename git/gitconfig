[user]
    name = Pietro Albini
    email = pietro@pietroalbini.org
    signingkey = 6F58BCDA
[core]
    excludesfile = ~/.gitignore
    editor = vim
    autocrlf = input
[commit]
    gpgsign = true
[push]
    default = simple
[alias]
    unstage = reset HEAD

# Specific configuration for GitHub
[url "git@github.com:"]
    # A pretty alias
    insteadof = "github:"

# Specific configuration for GitLab.com
[url "git@gitlab.com:"]
    # A pretty alias
    insteadof = "gitlab:"

# Specific configuration for code.ubuntu-it.org
[url "git@code.ubuntu-it.org:"]
    # A pretty alias
    insteadof = "uit:"

# Specific configuration for my personal git server
[url "git@git.me.pietroalbini.org:"]
    # A pretty alias
    insteadof = "me:"

[gpg]
	program = gpg2
