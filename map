# The symlinks map
#
# Syntax: Each line must have a symlink, and origin and dest must be separated
# by two spaces.

bash/bashrc  ~/.bashrc
bash/bashrc.d  ~/.bashrc.d

git/config  ~/.gitconfig
git/ignore  ~/.gitignore
