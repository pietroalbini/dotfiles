# The symlinks map
#
# Syntax: Each line must have a symlink, and origin and dest must be separated
# by two spaces.

bash/bashrc.d/workstation  ~/.bashrc.d/workstation

ssh/config  ~/.ssh/config

terminator  ~/.config/terminator

gpg/gpg.conf  ~/.gnupg/gpg.conf
gpg/gpg-agent.conf  ~/.gnupg/gpg-agent.conf

i3  ~/.config/i3
i3status  ~/.config/i3status
